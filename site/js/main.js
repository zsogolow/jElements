_(document).bind('DOMContentLoaded', function () {

    var smoothScroll = undefined;
    var lax = undefined;

    function init() {
        // smoothScroll = scroller(window);
        lax = parallax();
    }

    init();
});
